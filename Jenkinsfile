pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building Python app'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Python app'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Python app'
            }
        }
    }
}
